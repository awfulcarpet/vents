welcome to the help page

basic usage:

GET /help
	return this page

GET /intro
	go to intro page

GET /vents
	return the complete list of vents formatted with date

GET /vent
	does the same thing as GET /vents

GET /latest
	return the latest vent in the db

	Queries
	n integer
		get the last n vents instead of the very last vent from server

GET /json
	get the list of vents as a json

POST /vents
	saves new vent in the database

	body parameters
		anything passed in will be saved as a new vent in the database

POST /vent
	does the same thing as POST /vents
